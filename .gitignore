# Node modules
node_modules/
**/node_modules/

# Build outputs
dist/
build/
*.tsbuildinfo

# Environment files
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE / editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Docker volumes (if you mount volumes, but we keep them)
# postgres_data/ and redis_data/ are volumes, but if you mount them locally, you might want to ignore them.
# Since they are defined in docker-compose.yml as volumes, they are managed by Docker, so no need to ignore local folders unless you have mounted them.
# But if you have a local postgres_data/ folder, add:
# postgres_data/
# redis_data/

# Python virtual environment (Phase 4 will add Python microservice)
venv/
env/
ENV/
__pycache__/
*.pyc
*.pyo

# Coverage reports
coverage/
.nyc_output/

# Misc
*.tmp
*.temp